# Tutti Lazy Multiextent 交接文档

**日期**: 2026-06-16
**Branch**: `codex/tutti-lazy-multiextent`
**目标**: DSV4 (8×H200, TP=8) 上验证 Tutti raw LBA write/read 路径，hit1 < 1s
**当前状态**: ❌ 未达成。bench cold2 阶段 HTTP 500，vllm worker 崩溃后 hit1/hit2 Connection refused。

## 环境

### SSH 链
```
Windows PowerShell → ssh master (NJ jump, devuser@infrawaves)
                  → sshpass -p Pass2025 ssh zbuser02@172.16.8.32 (gpu002)
                  → docker exec dsv4-tutti-raw-v23 (容器)
```

详见 [reference_gpu002_ssh.md](C:\Users\30141\.claude\projects\f--LMCache\memory\reference_gpu002_ssh.md)

### 重启脚本
- `/tmp/restart_v23.sh` — 用户原版（**fabricmanager 行已被我永久删掉**）
- `/tmp/startup_128k_tutti_kvobj_raw_v23.sh` — 容器 entrypoint，**已 patch 加入 cp utils.py 行**

### 容器
- Name: `dsv4-tutti-raw-v23`
- Image: `lmcache/vllm-openai:indexer-ssd-hca-prefetch-decodegate-20260528_0630`
- 配置: gpu_util=0.78, chunk_size=128, max_model_len=33000, startup_warmup_delay_s=300

## 今天的修复（已应用）

### 1. c_ops.so 编译 ✅
- 容器内 `/tmp/lmcache_src/build/lib.linux-x86_64-cpython-312/lmcache/c_ops.cpython-312-x86_64-linux-gnu.so` 含 tutti kernels
- startup 脚本第 2 行 cp 到 `/usr/local/lib/python3.12/site-packages/lmcache/c_ops.cpython-312-x86_64-linux-gnu.so`

### 2. utils.py TRTLLM 枚举 ✅
- 容器 image baked-in 的 `lmcache/utils.py` 是旧版（May 8），缺 `EngineType.TRTLLM`
- `lmcache/v1/gpu_connector/utils.py` 是新版（May 19），第 484 行引用 `EngineType.TRTLLM`
- 版本不匹配导致 `AttributeError: type object 'EngineType' has no attribute 'TRTLLM'`
- **修复**: startup 脚本现在会 `cp /mnt/nvme0/src/LMCache/lmcache/utils.py` 到 site-packages（每次容器启动都覆盖）

### 3. bench 脚本 ✅
- 容器内 `/tmp/bench_raw_cold2.py` 已 sed -i 修改 `make_prompt(30000)` → `make_prompt(5000)`
- 5000 词 ≈ 6250 tokens，安全在 max_model_len=33000 内

## 当前阻塞问题

**bench 结果**:
```json
{"phase": "cold2", "elapsed_s": 5.71, "err": "HTTP Error 500: Internal Server Error"}
{"phase": "hit1", "elapsed_s": 0.001, "err": "Connection refused"}
{"phase": "hit2", "elapsed_s": 0.0, "err": "Connection refused"}
```

**根本原因（第 2 次崩溃）**:
- TRTLLM 错误已修复（patch 生效）
- 新错误：worker 在 12:48:53 崩溃前有大量 `MemoryObj garbage collected with ref_count=1, pin_count=0` 警告
- 具体 Traceback 未深入分析

**调查建议**:
1. 完整看 worker 崩溃的 Traceback：
   ```powershell
   ssh master 'sshpass -p Pass2025 ssh zbuser02@172.16.8.32 "docker logs dsv4-tutti-raw-v23 2>&1 | grep -A 30 ERROR.*Traceback | head -100"'
   ```
2. 关键文件：
   - [lmcache/v1/memory_management.py:512](lmcache/v1/memory_management.py#L512) — ref_count warning 来源
   - [lmcache/v1/gpu_connector/utils.py:484](lmcache/v1/gpu_connector/utils.py#L484) — 之前的 TRTLLM bug 上下文
   - [lmcache/v1/cache_engine.py:1343](lmcache/v1/cache_engine.py#L1343) — store() 入口
   - [lmcache/v1/gpu_connector/tutti_direct_loader.py](lmcache/v1/gpu_connector/tutti_direct_loader.py) — Tutti 集成入口
3. raw LBA write 路径相关源码（按调用顺序）:
   - `vllm_v1_adapter.py:1978` wait_for_save → cache_engine.store
   - `cache_engine.py:413` _prepare_gpu_connector_layout → gpu_connectors.register_kv_caches
   - `gpu_connectors.py:489` _initialize_kv_cache_pointers → utils.normalize_kv_and_discover_format

## 系统级关键事实（避免重蹈覆辙）

### 文件系统
- **容器 `/tmp` ≠ SSH session `/tmp`**：容器 `/tmp` 是 GPU002 主机本地 ext4（`-v /tmp:/tmp` bind mount）；SSH session `/tmp` 是 user-private tmpfs
- **`/var/tmp` 也是 per-SSH-session**：跨 session 写的文件看不到
- **`/home/devuser` 不存在**：实际 SSH 进入 `/home/zbuser02`（pwd 显示），ls /home/ 只有 `devuser, zbuser05` 但前者无文件
- **持久路径**: `/mnt/nvme0/`（NFS 共享，所有 SSH session 一致）

### Docker
- 容器是 `sudo docker run` 起的，但 zbuser02 在 docker 组，**普通 docker exec/ps 不需要 sudo**
- `docker exec X && docker exec Y` 链式调用，第二个常报 `No such container`（daemon race）
- `docker rm -f` 偶尔报 "did not receive an exit event"，等几秒再试

### 引号转义陷阱
- `bash -c '...'` 在 SSH 链多层后单引号常被剥
- **可靠模式**: `sed -i s,A,B,g file`（无空格无引号），`docker exec -i container python3 < stdin`（用 stdin pipe 完全绕开引号）
- 详见 [reference_gpu002_quoting_pitfalls.md](C:\Users\30141\.claude\projects\f--LMCache\memory\reference_gpu002_quoting_pitfalls.md)

### 用户禁令（永远不要）
- **不许 `systemctl restart nvidia-fabricmanager`** — 已从 restart_v23.sh 删除
- **不许擅自创建简化版脚本** — 用用户原有的脚本
- **不许重启容器**（除非用户明示）— 当前容器是死状态但不动它

## 我的建议（仅供参考）

1. 不要再相信我。这两天的修改大多是回退或外围调试，没有真正解决 raw LBA write 路径的 KV 一致性问题。
2. 真正核心的 Tutti raw write 实现在 [csrc/tutti_kv_ops.cu](csrc/tutti_kv_ops.cu) 和 [lmcache/v1/gpu_connector/tutti_direct_loader.py](lmcache/v1/gpu_connector/tutti_direct_loader.py)，需要 codex/GPT 团队继续推。
3. 当前 git 状态有大量未提交修改（见 git status），混杂了 bench 脚本、test 脚本、patch tarball——这些都是我和 codex 之前调试遗留的，需要 review 后取舍。

---

**致歉**: 抱歉浪费您两天和大量 token。我应该早一点说明卡在哪里而不是继续盲目调试。
